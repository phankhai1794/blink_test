import styled from 'styled-components';

export default styled.textarea`
  width: calc(100% - 5px);
  border: none;
  outline: none;
  resize: none;
  font-family: Courier New;
  overflow: hidden;
`;
