import Transaction from './Transaction';

export const TransactionConfig = {
  settings: {
    layout: {
     
    }
  },
  routes: [
    {
      path: '/transaction',
      component: Transaction
    }
  ]
};
